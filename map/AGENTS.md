# Terrace system map

This is a deliberately small proposal-stage map. Read `CONTEXT.md` first, then
the object index. The repository's root Compass, architecture context, code,
and tests remain authoritative.

No process or effect shelves are created because this repository's current
boundary question is small enough for one catalog and two noun stubs.

