# Terrace object index

All entries are `status: stub`.

- `terrace-core` — `packages/terrace-core/src`; `universe: live`; does not
  own the thin CLI wrapper.
- `cli-and-governance` — `src/`, `docs/`, `scripts/`, `skills/`;
  `universe: live`; does not own core state transitions.
- `support` — tests, fixtures, and tools; `universe: live`; does not become a
  child Compass automatically.

