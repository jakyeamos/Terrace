---
phase: 02-governance-workflows
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/agent-contract.test.ts
  - tests/fragment-index.test.ts
  - tests/workflow-interrogation.test.ts
  - tests/workflow-spec-compiler.test.ts
  - tests/workflow-test-architect.test.ts
  - tests/workflow-intake.test.ts
  - tests/spec-hash.test.ts
  - tests/fragment-loader.test.ts
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-07
  - AGNT-08
  - FRAG-01
  - FRAG-02
  - FRAG-03
  - FRAG-04
  - FRAG-05
  - FRAG-06
  - FRAG-07
  - FRAG-08
  - FRAG-09
  - WKFL-01
  - WKFL-02
  - WKFL-02a
  - WKFL-02b
  - WKFL-02c
  - WKFL-03
  - WKFL-04
  - WKFL-05
  - WKFL-06
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04

must_haves:
  truths:
    - "Phase 2 delta test slice exits non-zero (RED) before any implementation is written"
    - "Every Phase 2 requirement cluster has at least one failing assertion in a dedicated test file"
    - "Tests import from src/lib/fragment-loader.cjs and src/lib/spec-hash.cjs (modules that do not yet exist)"
    - "Prior-phase baseline tests remain GREEN throughout this plan"
  artifacts:
    - path: "tests/agent-contract.test.ts"
      provides: "RED tests for AGNT-07/08 contract fields and steering.md loader"
    - path: "tests/fragment-index.test.ts"
      provides: "RED tests for FRAG-01/02/03/04/05 fragment-index.json schema and tier loading"
    - path: "tests/workflow-interrogation.test.ts"
      provides: "RED tests for WKFL-02/02a/02b/02c/03 tri-modal interrogation and fast-mode path"
    - path: "tests/workflow-spec-compiler.test.ts"
      provides: "RED tests for WKFL-04/05 spec compilation and derived artifact updates"
    - path: "tests/workflow-test-architect.test.ts"
      provides: "RED tests for WKFL-06 test architecture with P0-P3 risk scoring"
    - path: "tests/workflow-intake.test.ts"
      provides: "RED tests for WKFL-01/OPS-01/02 intake and provisional PRD"
    - path: "tests/spec-hash.test.ts"
      provides: "RED tests for OPS-03/04 spec hash normalization and exclusions"
    - path: "tests/fragment-loader.test.ts"
      provides: "RED tests for FRAG-02/03/04 runtime fragment loading with tier filtering"
  key_links:
    - from: "tests/*.test.ts"
      to: "src/lib/fragment-loader.cjs"
      via: "require('../src/lib/fragment-loader.cjs')"
      pattern: "fragment-loader"
    - from: "tests/spec-hash.test.ts"
      to: "src/lib/spec-hash.cjs"
      via: "require('../src/lib/spec-hash.cjs')"
      pattern: "spec-hash"
---

<objective>
Wave 1 RED stubs: write the full Phase 2 acceptance test surface before any implementation exists.

Purpose: The TDD mandate is non-negotiable. Every implementation plan that follows must find failing Phase 2 delta tests waiting. This plan creates eight test files covering all 26 Phase 2 requirements. No production code is written here — only test stubs that fail against the current codebase because they import modules that do not yet exist (fragment-loader.cjs, spec-hash.cjs) and assert behaviors that are not yet implemented.

Output: Eight test files in tests/ covering agent contracts, fragment index schema, all four governance workflows, spec hash computation, and fragment loader runtime behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-governance-workflows/02-CONTEXT.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
</context>

<interfaces>
<!-- Established test patterns from Phase 1 — every Phase 2 test must follow these exactly. -->

From tests/validate.test.ts (pattern to replicate):
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

// Modules under test use require() (CJS)
const { validateArtifacts } = require('../src/lib/validate.cjs');

describe('feature name (REQ-ID list)', () => {
  let tmpDir: string;
  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'terrace-test-'));
    // set up subdirectories as needed
  });
  afterEach(() => { fs.rmSync(tmpDir, { recursive: true, force: true }); });
  it('does X (REQ-ID)', () => { ... });
});
```

From tests/json-mode.test.ts (CLI invocation pattern):
```typescript
import { execFileSync } from 'child_process';
const CLI = path.resolve(__dirname, '../src/terrace-tools.cjs');
// execFileSync('node', [CLI, 'spec', 'hash', '--file', specFile], { encoding: 'utf-8' })
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → CJS module | Tests import not-yet-existing modules; import error must surface as RED, not silent skip |
| tmpDir filesystem | Each test creates isolated temp dir; no shared global state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Vacuous test stubs | mitigate | Every test file must have at least one `expect()` call that fails against current state; no `it.todo()`, no `expect(true).toBe(false)` placeholders; executor must verify `pnpm test` reports failures |
| T-02-02 | Information Disclosure | tmpDir leakage | mitigate | `afterEach` always calls `fs.rmSync(tmpDir, { recursive: true, force: true })`; no shared temp paths across tests |
| T-02-03 | Tampering | Breaking prior-phase tests | mitigate | New test files must not modify any existing tests/ files; run `pnpm test` after writing each new file to confirm prior tests remain GREEN |
| T-02-04 | Elevation of Privilege | Shell injection in CLI tests | mitigate | Use `execFileSync('node', [CLI, ...args])` not `execSync` with shell:true; args array never contains user input |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Agent contract and steering loader RED tests</name>
  <files>tests/agent-contract.test.ts</files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/tests/validate.test.ts (copy the import pattern exactly)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (AGNT-07 field list at §11, AGNT-08)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-01 through D-04)
  </read_first>
  <behavior>
    - Test: loadAgentDefinition() throws MODULE_NOT_FOUND (RED — module does not exist yet)
    - Test: agent SKILL.md contains all AGNT-07 fields: purpose, allowed_outputs, forbidden_actions, required_inputs, handoff_behavior, artifact_ownership, fragment_index_ref (AGNT-07)
    - Test: first workflow step instruction in SKILL.md is 'Read `.terrace/steering.md` before any other step' (AGNT-08, D-03)
    - Test: agent directory exists at .agents/skills/terrace-spec-interrogator/ (AGNT-01, D-01)
    - Test: agent directory exists at .agents/skills/terrace-spec-compiler/ (AGNT-02, D-01)
    - Test: agent directory exists at .agents/skills/terrace-test-architect/ (AGNT-03, D-01)
    - Test: each agent SKILL.md has a fragment_index_ref field pointing to fragments/fragment-index.json (AGNT-07, D-02)
  </behavior>
  <action>
Create tests/agent-contract.test.ts. Import pattern: `const { loadAgentDefinition } = require('../src/lib/fragment-loader.cjs')` — this module does not exist yet, so the require will throw MODULE_NOT_FOUND. The first test must capture this thrown error to prove RED.

For tests that check on-disk SKILL.md content: use `fs.existsSync()` and `fs.readFileSync()` on known absolute paths relative to `process.cwd()`. These tests will fail RED because the SKILL.md files do not yet exist.

For AGNT-07 field tests, parse the SKILL.md as text and assert that each field name appears as a string in the file content. Use `expect(content).toContain('purpose:')`, etc.

Reference requirement IDs in each test description string, e.g., `it('SKILL.md contains all AGNT-07 fields (AGNT-07)', ...)`.
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose tests/agent-contract.test.ts 2>&1 | grep -E "FAIL|fail|Error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/agent-contract.test.ts exists on disk
    - File contains at least 7 `it(` blocks
    - Running `pnpm test tests/agent-contract.test.ts` exits non-zero (RED)
    - No test in this file uses `it.todo()` or `expect(true).toBe(false)`
    - All existing Phase 1 tests still pass: `pnpm test tests/validate.test.ts` exits 0
  </acceptance_criteria>
  <done>tests/agent-contract.test.ts exists, fails with meaningful errors (not syntax errors), and does not break prior tests.</done>
</task>

<task type="tdd">
  <name>Task 2: Fragment index, loader, and workflow RED tests</name>
  <files>
    tests/fragment-index.test.ts,
    tests/fragment-loader.test.ts,
    tests/workflow-interrogation.test.ts,
    tests/workflow-spec-compiler.test.ts,
    tests/workflow-test-architect.test.ts,
    tests/workflow-intake.test.ts,
    tests/spec-hash.test.ts
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/tests/validate.test.ts (import pattern)
    - /Users/jakyeamos/projects/Terrace/tests/json-mode.test.ts (execFileSync pattern for CLI tests)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (FRAG-01–09 at §7b; WKFL-01–06 at §9; OPS-01–04 at §14)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-05 through D-19)
  </read_first>
  <behavior>
    Fragment index tests (FRAG-01/02/05):
    - fragment-index.json at .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json exists (RED — file not yet written)
    - fragment-index.json has fields: agent (string), fragments (array with id, name, tags, tier, file)
    - tier values are only 'core', 'extended', or 'specialized'
    - fragment files are .md (FRAG-05)

    Fragment loader tests (FRAG-02/03/04):
    - loadFragments(agentDir, {tier: 'core'}) returns only core-tier fragment contents
    - loadFragments(agentDir, {tier: 'all'}) returns all tier fragments
    - loadFragments(agentDir, {tier: 'core'}).tokenCount is at least 40% less than loadFragments(agentDir, {tier: 'all'}).tokenCount (FRAG-04, MET-ERG-07) — assert ratio > 0.40
    - step-01 of a workflow reads fragment-index.json (FRAG-03) — assert fragment-index.json exists in agent dir before workflow step runs

    Interrogation workflow tests (WKFL-02/02a/02b/02c/03):
    - step-01-create.md exists in .agents/skills/terrace-spec-interrogator/ (D-05)
    - step-01-create.md YAML frontmatter has: step, mode, next_step, requires (D-06)
    - step-01-create.md mode field equals 'create' (D-06)
    - step-02-edit.md exists with mode 'edit' (WKFL-02b, D-05)
    - step-03-validate.md exists with mode 'validate' and next_step null (WKFL-02c, D-07)
    - CLARIFICATIONS.md path in fast-mode output is docs/prd/CLARIFICATIONS.md (D-16)
    - fast-mode assumption entry has fields: assumption, status: 'unresolved', resolution: 'deferred' (D-16, WKFL-03)

    Spec compiler workflow tests (WKFL-04/05):
    - step-01-create.md exists in .agents/skills/terrace-spec-compiler/
    - COMPILED-SPEC.md output has YAML frontmatter with: spec_version, project, phase, requirements, protected, last_updated, source_refs (WKFL-04)
    - derived artifacts path list includes INVARIANTS.md, PERMISSIONS-MATRIX.md, STATE-MACHINES.md (WKFL-05)

    Test architect workflow tests (WKFL-06):
    - step-01-create.md exists in .agents/skills/terrace-test-architect/
    - TEST-ARCH.md output maps requirement IDs to test layers with p0|p1|p2|p3 risk score (WKFL-06)
    - ci_tier field is present in TEST-ARCH.md requirement entries (WKFL-06)

    Intake workflow tests (WKFL-01/OPS-01/02):
    - When no PRD exists, intake produces docs/prd/PRD.md marked [PROVISIONAL] in unfilled sections (OPS-01, D-13)
    - Provisional PRD reads from .terrace/steering.md for intent, non_negotiables, scope_boundaries (D-14)
    - Stage 2 offer text contains exact string 'Run the Interrogation workflow to refine ambiguities? (y/N)' (D-15)
    - When interrogation declined, CLARIFICATIONS.md is created with unresolved assumptions (OPS-02, D-16)

    Spec hash tests (OPS-03/04):
    - computeSpecHash() is callable from src/lib/spec-hash.cjs (RED — module not yet written)
    - computeSpecHash() strips blank lines before hashing
    - computeSpecHash() strips trailing whitespace per line
    - computeSpecHash() strips lines that are comment-only (lines matching /^\s*<!--.*-->\s*$/)
    - computeSpecHash() excludes the last_updated: YAML frontmatter field
    - Two inputs differing only in whitespace produce the same hash (OPS-03)
    - `terrace spec hash --file <path>` CLI command is recognized (OPS-04, D-19)
  </behavior>
  <action>
Create each test file following the exact import pattern from tests/validate.test.ts:

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';
```

For CJS module imports (fragment-loader, spec-hash): place `const { ... } = require('../src/lib/MODULE.cjs')` inside the test body or in a try/catch at the top of the describe block so the file parses successfully but the test fails at runtime.

For on-disk file assertions (step files, fragment-index.json, SKILL.md): use `fs.existsSync(path.resolve(process.cwd(), 'RELATIVE_PATH'))`. These return false because files do not yet exist, making the `expect(result).toBe(true)` fail RED.

For fixture-based fragment loading tests (FRAG-04 ratio check): use the ts-monorepo fixture at `fixtures/ts-monorepo/` as the reference project. Create a minimal fragment-index.json in a tmpDir with two core fragments (each ~100 chars) and four extended fragments (each ~100 chars) to prove the 40% reduction math. The ratio assertion will fail RED because the loader module does not exist.

For workflow step-file YAML parsing tests: if the step files exist (they won't in Plan 02-01), parse them with a small inline YAML header extractor (extract content between `---` delimiters, split on `:`, no external YAML lib needed in tests). If files are absent, `fs.existsSync` returns false and the test fails RED.

Each test file must have a comment header naming the requirements it covers:
```
// Phase 2 RED stubs — covers FRAG-01, FRAG-02, FRAG-03, FRAG-04, FRAG-05
```
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new test files exist under tests/
    - `pnpm test` exits non-zero (Phase 2 delta tests are RED)
    - `pnpm test tests/validate.test.ts tests/init.test.ts tests/lifecycle.test.ts tests/preset.test.ts tests/doctor.test.ts tests/json-mode.test.ts tests/schemas.test.ts tests/templates.test.ts` exits 0 (Phase 1 baseline stays GREEN)
    - No new test file contains `it.todo()` or `expect(true).toBe(false)`
    - Each test file has at least one `expect()` call that is currently failing for a real reason (module not found, file not found, or assertion mismatch) — not a syntax error
    - Running `grep -r "it.todo" tests/` returns nothing
  </acceptance_criteria>
  <done>Seven test files exist and fail RED. Phase 1 tests remain GREEN. pnpm test --reporter=verbose shows Phase 2 failure names mapping to AGNT-07, FRAG-01–09, WKFL-01–06, OPS-01–04 requirement IDs.</done>
</task>

</tasks>

<verification>
1. `pnpm test` exits non-zero — Phase 2 delta slice is RED
2. `pnpm test tests/validate.test.ts tests/init.test.ts tests/lifecycle.test.ts tests/preset.test.ts tests/doctor.test.ts tests/json-mode.test.ts tests/schemas.test.ts tests/templates.test.ts` exits 0 — Phase 1 baseline GREEN
3. `ls tests/agent-contract.test.ts tests/fragment-index.test.ts tests/fragment-loader.test.ts tests/workflow-interrogation.test.ts tests/workflow-spec-compiler.test.ts tests/workflow-test-architect.test.ts tests/workflow-intake.test.ts tests/spec-hash.test.ts` — all 8 files exist
4. `grep -c "it(" tests/agent-contract.test.ts` — at least 7
5. `grep -r "it.todo" tests/` — empty output
</verification>

<success_criteria>
Eight RED test files cover all 26 Phase 2 requirements. Prior Phase 1 baseline tests remain GREEN. No implementation code exists yet.
</success_criteria>

<output>
After completion, create `.planning/phases/02-governance-workflows/02-01-SUMMARY.md`
</output>
