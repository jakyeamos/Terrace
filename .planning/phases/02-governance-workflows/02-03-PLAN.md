---
phase: 02-governance-workflows
plan: 03
type: tdd
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .agents/skills/terrace-spec-interrogator/step-01-create.md
  - .agents/skills/terrace-spec-interrogator/step-02-edit.md
  - .agents/skills/terrace-spec-interrogator/step-03-validate.md
  - .agents/skills/terrace-spec-interrogator/fragments/frg-int-01-question-rounds.md
  - .agents/skills/terrace-spec-interrogator/fragments/frg-int-02-assumption-logging.md
  - .agents/skills/terrace-spec-interrogator/fragments/frg-int-03-edge-case-probing.md
  - .agents/skills/terrace-spec-interrogator/fragments/frg-int-04-fast-mode.md
  - .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json
  - .agents/skills/terrace-spec-interrogator/SKILL.md
autonomous: true
requirements:
  - WKFL-02
  - WKFL-02a
  - WKFL-02b
  - WKFL-02c
  - WKFL-03
  - FRAG-06

must_haves:
  truths:
    - "Three step files exist for the Interrogation workflow with correct YAML frontmatter chaining next_step fields"
    - "step-01-create.md implements structured question rounds (goals, permissions, state transitions, edge cases, failure modes) and reads fragments by tier first"
    - "step-01-create.md fast-mode path creates docs/prd/CLARIFICATIONS.md with unresolved assumptions in the required schema"
    - "step-02-edit.md revises existing interrogation output without restarting"
    - "step-03-validate.md evaluates output against a completion checklist and produces pass/fail"
    - "CLARIFICATIONS.md entries have fields: assumption, status: 'unresolved', resolution: 'deferred'"
    - "Four FRAG-06 fragments cover question-round templates, assumption-logging, edge-case probing, and fast-mode path"
  artifacts:
    - path: ".agents/skills/terrace-spec-interrogator/step-01-create.md"
      provides: "Create mode: structured question rounds + fast-mode path"
      contains: "next_step:"
    - path: ".agents/skills/terrace-spec-interrogator/step-02-edit.md"
      provides: "Edit mode: revise existing interrogation output"
      contains: "next_step:"
    - path: ".agents/skills/terrace-spec-interrogator/step-03-validate.md"
      provides: "Validate mode: completion checklist, pass/fail report"
      contains: "next_step: null"
    - path: ".agents/skills/terrace-spec-interrogator/fragments/frg-int-04-fast-mode.md"
      provides: "Fast-mode path instructions and CLARIFICATIONS.md schema"
    - path: ".agents/skills/terrace-spec-interrogator/fragments/fragment-index.json"
      provides: "Updated catalog with all four FRAG-06 fragments"
      contains: "\"tier\""
  key_links:
    - from: "step-01-create.md"
      to: "step-02-edit.md"
      via: "next_step: step-02-edit.md in YAML frontmatter"
      pattern: "next_step: step-02-edit\\.md"
    - from: "step-02-edit.md"
      to: "step-03-validate.md"
      via: "next_step: step-03-validate.md in YAML frontmatter"
      pattern: "next_step: step-03-validate\\.md"
    - from: "step-01-create.md fast-mode"
      to: "docs/prd/CLARIFICATIONS.md"
      via: "explicit path reference in fast-mode instructions"
      pattern: "docs/prd/CLARIFICATIONS\\.md"
---

<objective>
Wave 2 implementation: terrace-spec-interrogator agent — step-file chain for the tri-modal Interrogation workflow plus the four FRAG-06 knowledge fragments.

Purpose: Build the complete Interrogation workflow (WKFL-02/02a/02b/02c) with step-file chaining (D-05/06/07/08), fast-mode assumption path (D-16/17), and unresolved-assumption logging (WKFL-03). The four FRAG-06 fragments give the interrogator agent the domain knowledge it needs for question-round structure, assumption schema, edge-case probing, and fast-mode bypass. Runs in parallel with Plans 02-04 and 02-05 (no shared files).

Output: Three step files, four fragment .md files, updated fragment-index.json, updated SKILL.md entry point. After this plan, workflow-interrogation.test.ts turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-governance-workflows/02-CONTEXT.md
@.planning/phases/02-governance-workflows/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Step file YAML frontmatter contract (D-06) — every step file MUST have these exact fields. -->

```yaml
---
step: <integer>
mode: create | edit | validate
next_step: <filename.md> | null
requires: <artifact or condition that must exist to run this step>
---
```

<!-- CLARIFICATIONS.md entry schema (D-16, WKFL-03) — each entry MUST use exactly these fields. -->

```markdown
## Assumption: <short title>

- assumption: <description of the assumption being made>
- status: unresolved
- resolution: deferred
```

<!-- fragment-index.json entry schema (D-10, FRAG-01) — from Plan 02-02. -->

```json
{ "id": "FRG-INT-XX", "name": "...", "tags": ["..."], "tier": "core", "file": "frg-int-XX-name.md" }
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step file → next step | next_step field drives resume; a broken filename silently loses progress |
| fast-mode path → CLARIFICATIONS.md | Unresolved assumptions must always be written; a missing file is silent data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Broken next_step chain | mitigate | step-01 next_step must be exactly "step-02-edit.md"; step-02 next_step must be exactly "step-03-validate.md"; step-03 next_step must be null; tests/workflow-interrogation.test.ts asserts these exact values |
| T-02-10 | Information Disclosure | Assumptions logged to wrong file | mitigate | fast-mode path instructions must hardcode the exact path "docs/prd/CLARIFICATIONS.md" — no variable path construction; test asserts this exact string in step-01-create.md content |
| T-02-11 | Repudiation | Unresolved assumption silently dropped | mitigate | fast-mode instructions require an entry for every assumption that was not answered; format is prescriptive (assumption + status + resolution fields); test asserts all three fields |
| T-02-12 | Denial of Service | Question rounds never terminate | mitigate | step-01-create.md must include an explicit early-stop rule: "Stop asking when no open assumptions remain below the threshold or when the user indicates complete"; WKFL-02a requires early-stop signal |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three step files for the tri-modal Interrogation workflow</name>
  <files>
    .agents/skills/terrace-spec-interrogator/step-01-create.md,
    .agents/skills/terrace-spec-interrogator/step-02-edit.md,
    .agents/skills/terrace-spec-interrogator/step-03-validate.md
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/.agents/skills/terrace-spec-interrogator/SKILL.md (existing agent definition — add entry-point routing to workflow section)
    - /Users/jakyeamos/projects/Terrace/tests/workflow-interrogation.test.ts (see exactly which assertions must pass)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (WKFL-02, WKFL-02a, WKFL-02b, WKFL-02c, WKFL-03 at §9)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-05 through D-08, D-16, D-17)
  </read_first>
  <behavior>
    step-01-create.md (WKFL-02, WKFL-02a, D-05/06):
    - YAML frontmatter: step=1, mode=create, next_step=step-02-edit.md, requires="docs/prd/PRD.md exists OR user request provided"
    - Workflow instructions load fragment-index.json then load all core-tier fragments before any question round
    - Runs five structured question rounds in order: (1) goals and success criteria, (2) actor permissions and roles, (3) state transitions and lifecycle events, (4) edge cases and boundary conditions, (5) failure modes and recovery paths
    - After each round: check if remaining ambiguity is below threshold; if yes, skip remaining rounds (early-stop per WKFL-02a)
    - Fast-mode path (D-16): if user declines to answer questions, immediately write docs/prd/CLARIFICATIONS.md with one entry per unresolved assumption using schema: assumption, status: unresolved, resolution: deferred
    - Output: docs/prd/CLARIFICATIONS.md (always written) and optionally docs/prd/EDGE-CASES.md

    step-02-edit.md (WKFL-02b, D-05/06):
    - YAML frontmatter: step=2, mode=edit, next_step=step-03-validate.md, requires="docs/prd/CLARIFICATIONS.md exists"
    - Workflow: read existing CLARIFICATIONS.md, identify sections marked [PROVISIONAL] or status: unresolved, revise only those sections without restarting question rounds from scratch
    - Does not re-run all five question rounds; targets only open assumptions

    step-03-validate.md (WKFL-02c, D-05/06/07):
    - YAML frontmatter: step=3, mode=validate, next_step=null, requires="docs/prd/CLARIFICATIONS.md exists"
    - Workflow: evaluate CLARIFICATIONS.md against a completion checklist:
      [ ] All five question-round topic areas have been addressed or explicitly logged as deferred
      [ ] No assumption entry has status: unresolved without a paired resolution rationale
      [ ] Edge cases cover at least boundary conditions and at least one failure mode
    - Output: pass/fail report written inline as a response (not a new file)
    - Fail report names exactly which checklist items are incomplete

    SKILL.md update (D-08):
    - Append to the Workflow section of SKILL.md the entry-point routing rule:
      "If no CLARIFICATIONS.md exists → read step-01-create.md. If CLARIFICATIONS.md exists → read step-02-edit.md. If validate mode explicitly requested → read step-03-validate.md."
  </behavior>
  <action>
Write step-01-create.md at `.agents/skills/terrace-spec-interrogator/step-01-create.md`. Start with the YAML frontmatter block (between --- delimiters), then a markdown body with the workflow instructions. The body must be written as agent-readable instructions (imperative prose), not as pseudocode.

Key content requirements for step-01-create.md body:
1. Opening line: "## Step 1: Create — Structured Interrogation"
2. Section "### Fragment Loading": "Read `fragments/fragment-index.json`. Load all fragments with tier: core before proceeding."
3. Section "### Question Rounds": list all five rounds by name with brief scope per round.
4. Section "### Early-Stop Rule": "After each round, assess remaining open assumptions. If no high-priority ambiguities remain, skip subsequent rounds and proceed to output."
5. Section "### Fast-Mode Path": "If the user declines to answer questions: write `docs/prd/CLARIFICATIONS.md`. For each unresolved assumption, add an entry with fields: `assumption:`, `status: unresolved`, `resolution: deferred`. Do not invent answers."
6. Section "### Output": list docs/prd/CLARIFICATIONS.md as mandatory output; docs/prd/EDGE-CASES.md as optional.

Write step-02-edit.md with the same structure. YAML frontmatter first. Body must reference the exact file path docs/prd/CLARIFICATIONS.md as the artifact to revise.

Write step-03-validate.md with the same structure. next_step value in YAML must be the literal string `null` (not quoted, just the YAML null value). Body must include the three-item checklist exactly as described in the behavior block above.

After writing all three files, update SKILL.md's Workflow section to add the entry-point routing rule (D-08). Read SKILL.md first, find the ## Workflow section, append the routing rule below the steering.md load instruction. Do not delete or replace any existing content.

Run `pnpm test tests/workflow-interrogation.test.ts` after writing. Fix failures before moving on.
  </action>
  <verify>
    <automated>pnpm test tests/workflow-interrogation.test.ts -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `ls .agents/skills/terrace-spec-interrogator/step-01-create.md .agents/skills/terrace-spec-interrogator/step-02-edit.md .agents/skills/terrace-spec-interrogator/step-03-validate.md` — all three exist
    - `grep "next_step: step-02-edit.md" .agents/skills/terrace-spec-interrogator/step-01-create.md` — returns match
    - `grep "next_step: step-03-validate.md" .agents/skills/terrace-spec-interrogator/step-02-edit.md` — returns match
    - `grep "next_step: null" .agents/skills/terrace-spec-interrogator/step-03-validate.md` — returns match
    - `grep "docs/prd/CLARIFICATIONS.md" .agents/skills/terrace-spec-interrogator/step-01-create.md` — returns match
    - `grep "status: unresolved" .agents/skills/terrace-spec-interrogator/step-01-create.md` — returns match
    - `grep "resolution: deferred" .agents/skills/terrace-spec-interrogator/step-01-create.md` — returns match
    - `grep "step-01-create.md\|step-02-edit.md\|step-03-validate.md" .agents/skills/terrace-spec-interrogator/SKILL.md` — all three referenced in routing rule
    - pnpm test tests/workflow-interrogation.test.ts — step-file existence, frontmatter, and CLARIFICATIONS schema tests GREEN
  </acceptance_criteria>
  <done>Three step files exist with correct YAML frontmatter chain. SKILL.md has entry-point routing. Interrogation workflow tests for step-file structure, next_step chaining, fast-mode path, and assumption schema all pass GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four FRAG-06 knowledge fragments for the interrogator agent</name>
  <files>
    .agents/skills/terrace-spec-interrogator/fragments/frg-int-01-question-rounds.md,
    .agents/skills/terrace-spec-interrogator/fragments/frg-int-02-assumption-logging.md,
    .agents/skills/terrace-spec-interrogator/fragments/frg-int-03-edge-case-probing.md,
    .agents/skills/terrace-spec-interrogator/fragments/frg-int-04-fast-mode.md,
    .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/.agents/skills/terrace-spec-interrogator/fragments/fragment-index.json (current state — add extended/specialized entries)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (FRAG-06 at §7b — list of required fragment topics)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-09 through D-12; Claude's Discretion for fragment content)
    - /Users/jakyeamos/projects/Terrace/tests/fragment-index.test.ts (schema assertions that must pass)
  </read_first>
  <behavior>
    FRAG-06 requires four fragment topics for the interrogator agent:
    - frg-int-01-question-rounds.md (core): structured templates for each of the five question rounds — goals, permissions, state transitions, edge cases, failure modes
    - frg-int-02-assumption-logging.md (core): assumption entry schema and logging discipline — when to log, what fields to include, how to distinguish deferred from resolved
    - frg-int-03-edge-case-probing.md (extended): patterns for identifying boundary conditions, compound state failure modes, and adversarial inputs
    - frg-int-04-fast-mode.md (specialized): fast-mode path instructions — when to trigger, exact CLARIFICATIONS.md format, what counts as "insufficient" for each question round

    Fragment index update: add frg-int-03 with tier: extended and frg-int-04 with tier: specialized to the existing fragment-index.json (which already has frg-int-01 and frg-int-02 as core entries from Plan 02-02).

    Token count behavior: loading only core tier (frg-int-01 + frg-int-02) must produce at least 40% fewer tokens than loading all four fragments. Each extended/specialized fragment must be substantive enough to matter — minimum 300 characters each.
  </behavior>
  <action>
Write each fragment file as substantive markdown. These are loaded as agent knowledge context — they must be useful, not placeholder text. Derive content from REQUIREMENTS.md and the 02-CONTEXT.md canonical refs section.

frg-int-01-question-rounds.md content structure:
- Heading: "# Question Round Templates"
- Section per round (Goals, Permissions, State Transitions, Edge Cases, Failure Modes)
- Each section: 3-5 specific questions to ask, formatted as a markdown list
- Total: at least 400 characters

frg-int-02-assumption-logging.md content structure:
- Heading: "# Assumption Logging Patterns"
- Schema block showing the exact CLARIFICATIONS.md entry format
- Rules: when to create an entry (any unanswered question → entry), what status values mean, distinction between deferred and resolved
- Total: at least 300 characters

frg-int-03-edge-case-probing.md content structure:
- Heading: "# Edge Case Probing Patterns"
- Boundary conditions section: value boundaries, state boundaries, actor permission edges
- Compound failures section: two concurrent state changes, partial failures
- Adversarial inputs section: empty inputs, max-length inputs, permission escalation attempts
- Total: at least 400 characters

frg-int-04-fast-mode.md content structure:
- Heading: "# Fast-Mode Path"
- Trigger condition: "User explicitly declines interrogation OR user has not responded after stating intent to skip"
- Required actions: list exact steps to write CLARIFICATIONS.md
- Example entry block showing the assumption/status/resolution format
- Total: at least 300 characters

After writing all four fragments, update fragment-index.json to add the two new entries (extended and specialized). Read the file first, parse the fragments array, append two new entries, write back. Do not remove the two existing core entries.

Run `pnpm test tests/fragment-index.test.ts tests/fragment-loader.test.ts` after updating. The FRAG-04 ratio test (40% reduction) should now pass because extended/specialized fragments add meaningful size.
  </action>
  <verify>
    <automated>pnpm test tests/fragment-index.test.ts tests/fragment-loader.test.ts tests/workflow-interrogation.test.ts -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls .agents/skills/terrace-spec-interrogator/fragments/frg-int-0{1,2,3,4}*.md` — all four files exist
    - `wc -c .agents/skills/terrace-spec-interrogator/fragments/frg-int-03-edge-case-probing.md` — at least 400 bytes
    - `wc -c .agents/skills/terrace-spec-interrogator/fragments/frg-int-04-fast-mode.md` — at least 300 bytes
    - `node -e "const idx=require('./.agents/skills/terrace-spec-interrogator/fragments/fragment-index.json'); console.log(idx.fragments.length)"` — prints 4
    - `node -e "const idx=require('./.agents/skills/terrace-spec-interrogator/fragments/fragment-index.json'); const tiers=idx.fragments.map(f=>f.tier); console.log(tiers)"` — prints array containing 'core', 'core', 'extended', 'specialized'
    - pnpm test tests/fragment-index.test.ts — all GREEN
    - pnpm test tests/fragment-loader.test.ts — FRAG-04 ratio test GREEN (loading core only is ≥40% smaller than loading all)
    - pnpm test tests/workflow-interrogation.test.ts — all previously-failing step-file tests now GREEN
  </acceptance_criteria>
  <done>Four FRAG-06 fragments exist with substantive content. fragment-index.json updated with extended and specialized tiers. FRAG-04 ratio assertion passes. All interrogation workflow tests GREEN.</done>
</task>

</tasks>

<verification>
1. `ls .agents/skills/terrace-spec-interrogator/step-0{1,2,3}*.md` — three files
2. `grep "next_step:" .agents/skills/terrace-spec-interrogator/step-01-create.md` — shows step-02-edit.md
3. `grep "next_step:" .agents/skills/terrace-spec-interrogator/step-03-validate.md` — shows null
4. `node -e "const idx=require('./.agents/skills/terrace-spec-interrogator/fragments/fragment-index.json'); console.log(idx.fragments.map(f=>f.tier))"` — ['core','core','extended','specialized']
5. `pnpm test tests/workflow-interrogation.test.ts tests/fragment-loader.test.ts` — all GREEN
6. `pnpm test tests/validate.test.ts` — still GREEN (Phase 1 baseline)
</verification>

<success_criteria>
Three step files implement the tri-modal Interrogation workflow with correct chaining. Four FRAG-06 fragments provide substantive knowledge content. Fragment-loader FRAG-04 ratio test passes. All workflow-interrogation.test.ts assertions GREEN. Phase 1 tests remain GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/02-governance-workflows/02-03-SUMMARY.md`
</output>
