---
phase: 02-governance-workflows
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/fragment-loader.cjs
  - .agents/skills/terrace-spec-interrogator/SKILL.md
  - .agents/skills/terrace-spec-compiler/SKILL.md
  - .agents/skills/terrace-test-architect/SKILL.md
  - .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json
  - .agents/skills/terrace-spec-compiler/fragments/fragment-index.json
  - .agents/skills/terrace-test-architect/fragments/fragment-index.json
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-07
  - AGNT-08
  - FRAG-01
  - FRAG-02
  - FRAG-03
  - FRAG-05

must_haves:
  truths:
    - "Three agent directories exist under .agents/skills/ each with a conforming SKILL.md"
    - "Every SKILL.md contains all seven AGNT-07 fields and an AGNT-08 steering.md load instruction as the first workflow step"
    - "Every agent has a fragments/fragment-index.json cataloguing core-tier fragments"
    - "fragment-loader.cjs loads core fragments unconditionally and returns their concatenated content"
    - "Core-tier fragments are loadable as markdown text and human-readable"
  artifacts:
    - path: ".agents/skills/terrace-spec-interrogator/SKILL.md"
      provides: "Interrogator agent definition with all AGNT-07 fields"
      contains: "fragment_index_ref"
    - path: ".agents/skills/terrace-spec-compiler/SKILL.md"
      provides: "Compiler agent definition with all AGNT-07 fields"
      contains: "fragment_index_ref"
    - path: ".agents/skills/terrace-test-architect/SKILL.md"
      provides: "Test architect agent definition with all AGNT-07 fields"
      contains: "fragment_index_ref"
    - path: ".agents/skills/terrace-spec-interrogator/fragments/fragment-index.json"
      provides: "Fragment catalog for interrogator agent"
      contains: "\"tier\""
    - path: "src/lib/fragment-loader.cjs"
      provides: "Runtime fragment loader with tier filtering"
      exports: ["loadFragments"]
  key_links:
    - from: "src/lib/fragment-loader.cjs"
      to: ".agents/skills/*/fragments/fragment-index.json"
      via: "fs.readFileSync on the fragment-index.json path"
      pattern: "fragment-index\\.json"
    - from: "each SKILL.md workflow section"
      to: ".terrace/steering.md"
      via: "hardcoded first instruction text"
      pattern: "Read `.terrace/steering.md`"
---

<objective>
Wave 2 implementation: agent definition framework — three governance agent SKILL.md files, their core-tier fragment indexes, and the fragment-loader.cjs runtime module.

Purpose: Establish the agent base contract (AGNT-07 fields + AGNT-08 steering load) and the core-tier fragment loading path that all subsequent plans depend on. The fragment-loader.cjs and SKILL.md files built here are the foundation that Plans 03, 04, and 05 extend with workflow step files and specialized fragments.

Output: Three agent SKILL.md files, three fragment-index.json files with core-tier entries, and src/lib/fragment-loader.cjs with loadFragments() that passes the agent-contract.test.ts and fragment-index.test.ts RED stubs from Plan 02-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-governance-workflows/02-CONTEXT.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
@.planning/phases/02-governance-workflows/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Established patterns from Phase 1 lib modules — fragment-loader.cjs must follow these exactly. -->

From src/lib/core.cjs (command registry pattern to replicate):
- Single CJS file, Node.js stdlib only (no npm dependencies)
- Exports object with named functions
- No class instantiation; functional style with explicit parameters

From src/lib/validate.cjs (function signature pattern):
```javascript
// Pattern: named function export, explicit rootDir parameter, returns structured result
function validateArtifacts(rootDir, config) { ... }
module.exports = { validateArtifacts };
```

From .agents/skills/gitnexus/ (existing skill directory — confirms directory and SKILL.md pattern):
- Agent skill directory at .agents/skills/{agent-name}/
- SKILL.md at root of agent directory
- SKILL.md uses GSD-style YAML frontmatter + purpose + trigger + workflow sections
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fragment-loader → filesystem | Reads fragment-index.json and fragment .md files; paths must be resolved against a known agent directory, never against user-supplied absolute paths |
| SKILL.md workflow instructions | Text content read by AI agents; must not contain executable code or shell commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Path traversal in fragment-loader | mitigate | fragment-loader.cjs resolves all paths with `path.resolve(agentDir, 'fragments', entry.file)` and verifies the resolved path starts with `agentDir` before reading; throw if path escapes agent dir |
| T-02-06 | Information Disclosure | Fragment content exposure | accept | Fragments are markdown knowledge files committed to the repo; no secrets, no credentials; ASVS L1 does not require confidentiality for committed knowledge artifacts |
| T-02-07 | Denial of Service | Oversized fragment files blocking context budget | mitigate | fragment-loader.cjs returns a `tokenCount` estimate (word count × 1.3) alongside content so callers can enforce MET-ERG-06 budget; loader itself does not truncate |
| T-02-08 | Tampering | SKILL.md missing AGNT-07 fields | mitigate | tests/agent-contract.test.ts (Plan 02-01) asserts all seven fields are present; executor must run pnpm test after writing each SKILL.md |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three agent SKILL.md files with AGNT-07/08 contract</name>
  <files>
    .agents/skills/terrace-spec-interrogator/SKILL.md,
    .agents/skills/terrace-spec-compiler/SKILL.md,
    .agents/skills/terrace-test-architect/SKILL.md
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/.agents/skills/gitnexus/SKILL.md (existing skill format — replicate structure)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (AGNT-07 field list at §11, AGNT-08, AGNT-01/02/03)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-01 through D-04)
    - /Users/jakyeamos/projects/Terrace/tests/agent-contract.test.ts (see exactly what assertions must pass)
  </read_first>
  <behavior>
    - SKILL.md for each agent contains all seven AGNT-07 fields in YAML frontmatter or labeled sections: purpose, allowed_outputs, forbidden_actions, required_inputs, handoff_behavior, artifact_ownership, fragment_index_ref
    - fragment_index_ref value is the literal path 'fragments/fragment-index.json' (relative to agent dir)
    - The workflow section's first instruction is the exact string: "Read `.terrace/steering.md` before any other step." (AGNT-08, D-03)
    - terrace-spec-interrogator purpose: "Reduce ambiguity in a feature request through structured question rounds, producing interrogation output and logging unresolved assumptions to docs/prd/CLARIFICATIONS.md"
    - terrace-spec-compiler purpose: "Convert interrogation output and PRD into compiled spec artifacts: COMPILED-SPEC.md, INVARIANTS.md, PERMISSIONS-MATRIX.md, STATE-MACHINES.md"
    - terrace-test-architect purpose: "Design the test matrix mapping each requirement to a test layer with P0-P3 risk score and CI tier assignment"
    - allowed_outputs for each agent lists the exact file paths the agent may write (no others)
    - forbidden_actions for each agent includes: "Do not modify .terrace/steering.md", "Do not modify source code files"
    - artifact_ownership declares which agent owns which artifact and which are shared read-only
  </behavior>
  <action>
Create the directory structure first: `mkdir -p .agents/skills/terrace-spec-interrogator/fragments .agents/skills/terrace-spec-compiler/fragments .agents/skills/terrace-test-architect/fragments`

Write each SKILL.md using the GSD-style format (YAML frontmatter block, then labeled sections). Use exactly this structure for each file:

```markdown
---
agent: terrace-spec-{NAME}
version: "1.0"
phase: governance
purpose: "{PURPOSE TEXT}"
allowed_outputs:
  - "{FILE PATH 1}"
  - "{FILE PATH 2}"
forbidden_actions:
  - "Do not modify .terrace/steering.md"
  - "Do not modify source code files"
  - "Do not write to any path not listed in allowed_outputs"
required_inputs:
  - ".terrace/steering.md"
  - "{WORKFLOW-SPECIFIC INPUT}"
handoff_behavior: "{DESCRIPTION OF WHAT TO PASS TO NEXT WORKFLOW STEP}"
artifact_ownership:
  owned: ["{LIST OF FILES THIS AGENT CREATES}"]
  reads: [".terrace/steering.md", "{OTHER READ-ONLY INPUTS}"]
fragment_index_ref: "fragments/fragment-index.json"
---

## Workflow

Read `.terrace/steering.md` before any other step.

{Remainder of workflow instructions — entry point routing, step file selection.
 For Plan 02-02 these can be stubs: "Step files will be added in Plans 02-03 through 02-05.
 Entry point: read step-01-create.md if no prior output exists; step-02-edit.md if output exists;
 step-03-validate.md if validate mode requested."}
```

Per D-04: the Phase 1 skill stubs (SKIL-01–27) that map to these agents already exist as scaffold files. Read each relevant stub first; if it exists, replace it with the full SKILL.md content above. If the stub does not exist, create the file fresh. Do not leave stub placeholder text in any SKILL.md this plan writes.

terrace-spec-interrogator allowed_outputs: ["docs/prd/CLARIFICATIONS.md", "docs/prd/EDGE-CASES.md"]
terrace-spec-compiler allowed_outputs: ["docs/spec/COMPILED-SPEC.md", "docs/spec/INVARIANTS.md", "docs/spec/PERMISSIONS-MATRIX.md", "docs/spec/STATE-MACHINES.md"]
terrace-test-architect allowed_outputs: ["docs/testing/TEST-ARCH.md", "docs/testing/COVERAGE-PLAN.md"]
  </action>
  <verify>
    <automated>pnpm test tests/agent-contract.test.ts -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls .agents/skills/terrace-spec-interrogator/SKILL.md .agents/skills/terrace-spec-compiler/SKILL.md .agents/skills/terrace-test-architect/SKILL.md` — all three files exist
    - `grep -c "fragment_index_ref" .agents/skills/terrace-spec-interrogator/SKILL.md` returns 1
    - `grep "Read \`.terrace/steering.md\`" .agents/skills/terrace-spec-interrogator/SKILL.md` finds the exact string
    - Same steering.md check passes for compiler and test-architect SKILL.md files
    - `grep -c "Do not modify .terrace/steering.md" .agents/skills/terrace-spec-interrogator/SKILL.md` returns 1
    - pnpm test tests/agent-contract.test.ts shows the agent-directory-existence and SKILL.md-field tests passing GREEN (other tests in this file may remain RED until later plans)
  </acceptance_criteria>
  <done>Three SKILL.md files on disk. Agent-contract tests for directory existence and AGNT-07/08 field presence pass GREEN. Tests that assert on step files or fragment content remain RED (those are Plan 02-03 and 02-06 scope).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fragment-index.json for each agent and fragment-loader.cjs</name>
  <files>
    .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json,
    .agents/skills/terrace-spec-compiler/fragments/fragment-index.json,
    .agents/skills/terrace-test-architect/fragments/fragment-index.json,
    src/lib/fragment-loader.cjs
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/src/lib/core.cjs (CJS module pattern — replicate exactly)
    - /Users/jakyeamos/projects/Terrace/src/lib/validate.cjs (function signature and export pattern)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (FRAG-01 schema at §7b; FRAG-02/03/04/05)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/02-governance-workflows/02-CONTEXT.md (D-09 through D-12)
    - /Users/jakyeamos/projects/Terrace/tests/fragment-index.test.ts (see exactly what assertions must pass)
    - /Users/jakyeamos/projects/Terrace/tests/fragment-loader.test.ts (see exactly what assertions must pass)
  </read_first>
  <behavior>
    fragment-index.json schema (D-10, FRAG-01):
    - Top-level fields: agent (string), fragments (array)
    - Each fragment entry: id (string matching /^FRG-\d+$/), name (string), tags (string[]), tier ('core'|'extended'|'specialized'), file (string ending in .md)
    - In Plan 02-02, each agent's fragment-index.json has at least 2 core-tier entries (extended/specialized entries added in Plan 02-03 through 02-05)
    - Fragment .md files named in the index exist on disk as readable markdown (FRAG-05)

    fragment-loader.cjs behavior (FRAG-02/03):
    - loadFragments(agentDir, options) where options.tier = 'core'|'extended'|'all'
    - When tier='core': reads fragment-index.json, filters to core entries, reads each .md file, returns { fragments: FragmentResult[], tokenCount: number }
    - When tier='all': loads all tiers
    - tokenCount is computed as Math.ceil(totalCharCount / 4) (standard LLM token approximation)
    - Path traversal guard: resolved path must start with path.resolve(agentDir); throw Error('UNSAFE_PATH') if not
    - Returns empty fragments array (not an error) when fragment-index.json has no entries for requested tier
  </behavior>
  <action>
Step 1 — Write each fragment-index.json. For Plan 02-02, populate with the minimum core-tier entries required to make fragment-index.test.ts assertions pass. Each index needs 2 core entries. Use sequential IDs per agent:

terrace-spec-interrogator: FRG-INT-01, FRG-INT-02 (core); files: frg-int-01-question-rounds.md, frg-int-02-assumption-logging.md
terrace-spec-compiler: FRG-CMP-01, FRG-CMP-02 (core); files: frg-cmp-01-spec-frontmatter.md, frg-cmp-02-invariant-patterns.md
terrace-test-architect: FRG-TST-01, FRG-TST-02 (core); files: frg-tst-01-test-layers.md, frg-tst-02-risk-scoring.md

Also create placeholder .md files for each (these receive real content in Plans 02-03/04/05). Content for now: a single markdown heading matching the fragment name plus one sentence description. Minimum 50 characters each so tokenCount > 0.

Step 2 — Write src/lib/fragment-loader.cjs following the exact CJS pattern from src/lib/core.cjs:

```javascript
'use strict';
const fs = require('fs');
const path = require('path');

/**
 * Load knowledge fragments for a governance agent.
 * @param {string} agentDir - Absolute path to agent skill directory
 * @param {{ tier: 'core' | 'extended' | 'specialized' | 'all' }} options
 * @returns {{ fragments: Array<{id: string, name: string, tier: string, content: string}>, tokenCount: number }}
 */
function loadFragments(agentDir, options) {
  const tier = (options && options.tier) || 'core';
  const indexPath = path.resolve(agentDir, 'fragments', 'fragment-index.json');
  const index = JSON.parse(fs.readFileSync(indexPath, 'utf-8'));

  const entries = index.fragments.filter(e =>
    tier === 'all' ? true : e.tier === tier
  );

  let totalChars = 0;
  const fragments = entries.map(entry => {
    const fragPath = path.resolve(agentDir, 'fragments', entry.file);
    // Path traversal guard
    if (!fragPath.startsWith(path.resolve(agentDir))) {
      throw new Error('UNSAFE_PATH: fragment file escapes agent directory');
    }
    const content = fs.readFileSync(fragPath, 'utf-8');
    totalChars += content.length;
    return { id: entry.id, name: entry.name, tier: entry.tier, content };
  });

  return {
    fragments,
    tokenCount: Math.ceil(totalChars / 4)
  };
}

module.exports = { loadFragments };
```

Run `pnpm test tests/fragment-index.test.ts tests/fragment-loader.test.ts` after writing. Fix any failures before moving on.
  </action>
  <verify>
    <automated>pnpm test tests/fragment-index.test.ts tests/fragment-loader.test.ts -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls .agents/skills/terrace-spec-interrogator/fragments/fragment-index.json` — file exists
    - Same check for compiler and test-architect
    - `node -e "const f=require('./src/lib/fragment-loader.cjs'); console.log(typeof f.loadFragments)"` prints 'function'
    - pnpm test tests/fragment-index.test.ts — exits 0 (all fragment-index schema assertions GREEN)
    - pnpm test tests/fragment-loader.test.ts — core-tier loading tests GREEN; FRAG-04 ratio test may remain RED until Plans 02-03/05 add extended/specialized fragments
    - `grep "UNSAFE_PATH" src/lib/fragment-loader.cjs` returns 1 match (path traversal guard present)
    - pnpm test tests/validate.test.ts tests/init.test.ts — still exits 0 (Phase 1 baseline unchanged)
  </acceptance_criteria>
  <done>Three fragment-index.json files on disk with core entries. Corresponding placeholder .md fragment files exist. fragment-loader.cjs exports loadFragments() and passes fragment-index and fragment-loader test assertions for core-tier loading.</done>
</task>

</tasks>

<verification>
1. `ls .agents/skills/terrace-spec-interrogator/SKILL.md .agents/skills/terrace-spec-compiler/SKILL.md .agents/skills/terrace-test-architect/SKILL.md` — all exist
2. `grep "fragment_index_ref" .agents/skills/terrace-spec-interrogator/SKILL.md .agents/skills/terrace-spec-compiler/SKILL.md .agents/skills/terrace-test-architect/SKILL.md` — each file has the field
3. `grep "Read \`.terrace/steering.md\`" .agents/skills/terrace-spec-interrogator/SKILL.md` — exact string present
4. `node -e "require('./src/lib/fragment-loader.cjs')"` — no error
5. `pnpm test tests/agent-contract.test.ts tests/fragment-index.test.ts tests/fragment-loader.test.ts` — directory/SKILL.md/schema assertions pass; step-file and ratio assertions remain RED
6. `pnpm test tests/validate.test.ts` — GREEN (Phase 1 baseline intact)
</verification>

<success_criteria>
Three conforming agent SKILL.md files and fragment-index.json catalogs exist. fragment-loader.cjs is callable and passes core-tier loading tests. AGNT-07/08 test assertions turn GREEN. Phase 1 baseline tests remain GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/02-governance-workflows/02-02-SUMMARY.md`
</output>
