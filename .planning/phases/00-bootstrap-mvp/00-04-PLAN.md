---
phase: 00-bootstrap-mvp
plan: 04
type: execute
wave: 3
depends_on:
  - 00-02
  - 00-03
files_modified: []
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - TMPL-12
  - TERR-10
  - TERR-11
  - OPS-08
  - OPS-09

must_haves:
  truths:
    - "Full Vitest suite (all 6 test files) passes GREEN"
    - "`terrace init` runs without error on all four fixture repos"
    - "`terrace spec validate` correctly reports errors and warnings on all four fixture repos"
    - "`terrace baseline protect` registers a test file with spec_ref on ts-monorepo and no-tests fixtures"
    - "`terrace session start` and `terrace session end` produce SESSION.md on all four fixture repos"
    - "Complete end-to-end loop (init → validate → baseline protect → session start → session end) completes on ts-monorepo without manual file repair"
  artifacts:
    - path: "terrace-tools.cjs"
      provides: "all five commands working correctly on all fixture repos"
    - path: "tests/init.test.ts"
      provides: "GREEN — all terrace init assertions pass"
    - path: "tests/templates.test.ts"
      provides: "GREEN — all five template assertions pass"
    - path: "tests/validate.test.ts"
      provides: "GREEN — ERROR:/WARN: behavior verified"
    - path: "tests/baseline.test.ts"
      provides: "GREEN — spec_ref validation and registry write verified"
    - path: "tests/session.test.ts"
      provides: "GREEN — session start/end and SESSION.md content verified"
    - path: "tests/fixtures.test.ts"
      provides: "GREEN — all four fixture repos exist"
  key_links:
    - from: "tests/*.test.ts"
      to: "terrace-tools.cjs"
      via: "runTerrace() helper — full suite exercises all commands"
      pattern: "npx vitest run"
    - from: "end-to-end loop"
      to: "fixtures/ts-monorepo (copied to tmpDir)"
      via: "sequential: init → validate-source → baseline-protect → session-start → session-end"
      pattern: "ts-monorepo"
---

<objective>
Run the complete Vitest suite against all fixture repos to confirm full GREEN state, then run a timed end-to-end loop on the ts-monorepo fixture to verify success criteria 6 from ROADMAP.md (complete loop without manual file repair).

Purpose: TDD Wave 3 (GREEN gate). Plans 00-02 and 00-03 turned individual test files GREEN. This plan confirms all tests pass together and the complete governance loop works end-to-end as a system.

Output: Full GREEN Vitest suite. Human-verified end-to-end run on ts-monorepo within MET-ERG-02 time budget. Phase 0 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-bootstrap-mvp/00-CONTEXT.md
@.planning/phases/00-bootstrap-mvp/00-02-SUMMARY.md
@.planning/phases/00-bootstrap-mvp/00-03-SUMMARY.md

<interfaces>
<!-- All commands available after 00-02 and 00-03: -->
<!-- node terrace-tools.cjs init -->
<!-- node terrace-tools.cjs validate-source -->
<!-- node terrace-tools.cjs baseline-protect <file> --spec-ref <ID> -->
<!-- node terrace-tools.cjs session-start -->
<!-- node terrace-tools.cjs session-end -->

<!-- Fixture repos at (git-tracked — copy to tmpDir before CLI operations): -->
<!-- fixtures/ts-monorepo   — TypeScript monorepo skeleton -->
<!-- fixtures/script-repo   — plain script files, no framework -->
<!-- fixtures/no-tests      — source files, no test files -->
<!-- fixtures/gsd-modified  — .claude/ + GSD configuration -->

<!-- Success criterion 6 from ROADMAP.md §Phase 0: -->
<!-- "A complete end-to-end run (init → intake → validate → baseline protect → session start → session end) -->
<!--  completes on the ts-monorepo fixture without manual file repair" -->
<!-- NOTE: "intake" in Phase 0 = init creates PRD.md automatically (D-04) — no separate intake command -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite GREEN — diagnose and fix any remaining RED tests</name>
  <files>terrace-tools.cjs</files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/terrace-tools.cjs (read full file — understand current state before diagnosing failures)
    - /Users/jakyeamos/projects/Terrace/tests/init.test.ts
    - /Users/jakyeamos/projects/Terrace/tests/validate.test.ts
    - /Users/jakyeamos/projects/Terrace/tests/baseline.test.ts
    - /Users/jakyeamos/projects/Terrace/tests/session.test.ts
    - /Users/jakyeamos/projects/Terrace/tests/templates.test.ts
    - /Users/jakyeamos/projects/Terrace/tests/fixtures.test.ts
    - /Users/jakyeamos/projects/Terrace/.planning/phases/00-bootstrap-mvp/00-VALIDATION.md (per-task verification map for expected pass/fail state)
  </read_first>
  <action>
    Run the complete Vitest suite:
    ```
    npx vitest run 2>&1
    ```

    For each failing test:
    1. Read the failing test assertion carefully
    2. Read the corresponding CLI handler in terrace-tools.cjs
    3. Identify the mismatch (output format, exit code, file path, missing field)
    4. Fix terrace-tools.cjs (the test is the specification — do not change tests)
    5. Re-run the specific test file to confirm GREEN
    6. Repeat until full suite passes

    Common failure modes to check:
    - Output format mismatches: test expects `CREATED .terrace/policy.json` but code prints absolute path → fix to relative path
    - validate-source section strings not matching templates: read both template file and SOURCE_ARTIFACTS map, align strings exactly
    - baseline-registry.json path wrong: must be relative to process.cwd(), not __dirname
    - SESSION.md path wrong: must be relative to process.cwd()
    - project-state.json missing a required field: check TMPL-12 field list (current_phase, spec_hash, active_slice, last_session, policy_mode)
    - tests/templates.test.ts assertion checks file created by init: if test copies fixture then runs init, the template file path must match where init writes it

    DO NOT modify test files. All fixes go in terrace-tools.cjs.
    DO NOT add external dependencies to fix failures.

    After each fix, run only the affected test file to confirm GREEN before running full suite again.
    Final verification: `npx vitest run` exits 0 with all tests PASS.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/projects/Terrace && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (ALL tests PASS GREEN)
    - Output contains no "FAIL" lines
    - Test count: all 6 test files pass (init, templates, validate, baseline, session, fixtures)
    - `npx vitest run` completes in under 30 seconds
    - `git status` shows no untracked files under `fixtures/` (tests cleaned up their tmpDirs)
    - `grep "it\.skip\|describe\.skip" tests/*.test.ts` returns empty (no skipped tests)
  </acceptance_criteria>
  <done>Complete Vitest suite passes GREEN; zero skipped tests; no test leaves artifacts in fixtures/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verified end-to-end loop on ts-monorepo fixture (Success Criterion 6)</name>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/.planning/ROADMAP.md §Phase 0 Success Criteria (especially criterion 6)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/00-bootstrap-mvp/00-VALIDATION.md (Manual-Only Verifications section)
  </read_first>
  <what-built>
    Task 1 confirmed the full automated test suite passes GREEN. Now verify the complete governance loop works as a connected system on the ts-monorepo fixture, within the MET-ERG-02 time budget (< 10 minutes for intake → compiled-spec workflow; the Phase 0 loop should be much faster).

    The loop to verify (all commands run in a copy of fixtures/ts-monorepo):

    Step 1: Copy fixture to tmpDir
    ```
    T=$(mktemp -d) && cp -r fixtures/ts-monorepo/. "$T/"
    echo "Working in: $T"
    ```

    Step 2: Init (creates .terrace/ skeleton and docs/ with 5 templates)
    ```
    node terrace-tools.cjs init
    ```
    Expected: CREATED lines for all 7 files, no errors

    Step 3: Validate freshly-initialized repo (docs exist and are valid)
    ```
    node terrace-tools.cjs validate-source
    ```
    Expected: exits 0 with no ERROR or WARN output (freshly created templates must be valid)

    Step 4: Baseline protect a source artifact
    ```
    node terrace-tools.cjs baseline-protect docs/spec/COMPILED-SPEC.md --spec-ref CS-001
    ```
    Expected: PROTECTED message, baseline-registry.json exists with valid entry

    Step 5: Session start
    ```
    node terrace-tools.cjs session-start
    ```
    Expected: SESSION STARTED message, .planning/sessions/SESSION.md exists

    Step 6: Session end
    ```
    node terrace-tools.cjs session-end
    ```
    Expected: SESSION ENDED message, SESSION.md has appended end block

    Run each command with `time` prefix to measure duration. Total loop must complete well under 10 minutes.
  </what-built>
  <how-to-verify>
    Run the following sequence in your terminal (from the Terrace repo root):

    ```bash
    # Create clean tmpDir with ts-monorepo fixture
    T=$(mktemp -d) && cp -r fixtures/ts-monorepo/. "$T/" && cd "$T"

    # Time the full loop
    time (
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs init &&
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs validate-source &&
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs baseline-protect docs/spec/COMPILED-SPEC.md --spec-ref CS-001 &&
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs session-start &&
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs session-end
    )

    # Verify artifacts on disk
    ls .terrace/ docs/prd/ docs/spec/ docs/testing/ .planning/sessions/
    cat .terrace/baseline-registry.json
    cat .planning/sessions/SESSION.md
    ```

    Check each of the following:
    1. `terrace init` prints CREATED lines for at least 7 files, no errors, exits 0
    2. `terrace validate-source` exits 0 with NO output (freshly initialized artifacts must be clean — no errors, no warnings)
    3. `terrace baseline protect` exits 0 and prints PROTECTED; `.terrace/baseline-registry.json` contains `"spec_ref": "CS-001"`
    4. `terrace session start` exits 0 and `.planning/sessions/SESSION.md` exists
    5. `terrace session end` exits 0 and `cat .planning/sessions/SESSION.md` shows appended end block
    6. Total elapsed time is under 2 minutes (well within MET-ERG-02 budget)
    7. `git status` at Terrace repo root shows NO new untracked files under `fixtures/` (tmpDir was separate)

    Also verify on the other three fixtures (less critical — spot check):
    ```bash
    for FIXTURE in script-repo no-tests gsd-modified; do
      T=$(mktemp -d) && cp -r /Users/jakyeamos/projects/Terrace/fixtures/$FIXTURE/. "$T/" && cd "$T"
      node /Users/jakyeamos/projects/Terrace/terrace-tools.cjs init && echo "$FIXTURE: OK" || echo "$FIXTURE: FAILED"
    done
    ```
    Expected: all three print OK
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 checks above pass.
    Describe any failures if checks do not pass — include the exact output and which step failed.
    If validate-source emits WARN on a freshly-initialized repo, describe exactly which file and section triggered it — this indicates a template/validator mismatch (Pitfall 5) that must be fixed before Phase 0 is complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture copy → tmpDir | End-to-end test copies fixtures/ to OS tmpdir; no writes to git-tracked fixture dirs |
| terrace-tools.cjs → all four fixture repos | CLI runs against fixture content; must not write back to fixtures/ in any test path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-14 | Tampering | End-to-end run writing to fixtures/ | mitigate | End-to-end loop MUST use `cp -r fixtures/ts-monorepo/. $(mktemp -d)/` — never run CLI commands directly in fixtures/ directory; `git status` check in acceptance criteria catches any slip |
| T-00-15 | Denial of Service | Vitest suite leaving tmpDirs on failure | accept | afterEach cleanup handles normal cases; OS will reclaim /tmp on reboot; no sensitive data in test tmpDirs |
| T-00-16 | Information Disclosure | SESSION.md written to wrong path | mitigate | Success criterion 5 explicitly verifies SESSION.md appears at `.planning/sessions/SESSION.md` inside tmpDir, not at CLI's __dirname — caught by human verification step |
</threat_model>

<verification>
Final Phase 0 verification checklist:

1. Full automated suite GREEN:
   ```
   npx vitest run
   ```
   Expected: exits 0, all 6 test files PASS, no skipped tests

2. No fixture pollution:
   ```
   git status fixtures/
   ```
   Expected: `nothing to commit` or no output (fixtures/ is clean)

3. End-to-end loop verified by human (Task 2 checkpoint):
   - init creates all artifacts
   - validate-source exits 0 with no output on fresh init
   - baseline-protect writes registry entry
   - session start/end produce SESSION.md
   - Total time < 10 minutes

4. All Phase 0 requirements satisfied:
   - TMPL-01 through TMPL-05: templates exist with required sections (GREEN in templates.test.ts)
   - TMPL-12: project-state.json schema created by init (GREEN in init.test.ts)
   - TERR-10: all four fixture repos exist (GREEN in fixtures.test.ts)
   - TERR-11 partial: fixture-based tests verify session and validation (GREEN in validate/session tests)
   - OPS-08: no-tests fixture works with terrace init and validate-source
   - OPS-09: language-agnostic — script-repo fixture works without TypeScript assumptions
</verification>

<success_criteria>
- `npx vitest run` exits 0 — all 6 test files PASS GREEN, zero skipped
- Human-verified end-to-end loop on ts-monorepo completes without manual file repair (Success Criterion 6 from ROADMAP.md)
- `terrace validate-source` on a freshly-initialized fixture exits 0 with no output (Pitfall 5 never manifested)
- All four fixture repos (ts-monorepo, script-repo, no-tests, gsd-modified) accept `terrace init` without error
- `git status fixtures/` is clean (no test artifacts in git-tracked directories)
- Phase 1 can begin: Phase 0 deliverables are on disk and verified
</success_criteria>

<output>
After completion, create `.planning/phases/00-bootstrap-mvp/00-04-SUMMARY.md` with:
- Final test suite state (pass count, file breakdown)
- End-to-end loop elapsed time on ts-monorepo
- Confirmed fixture repo coverage (all four)
- Any issues found and fixed during this plan
- Explicit statement: "Phase 0 complete — Phase 1 may begin"
</output>
