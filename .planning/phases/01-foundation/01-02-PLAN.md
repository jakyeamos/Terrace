---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/templates/PRD.md
  - src/templates/COMPILED-SPEC.md
  - src/templates/TEST-ARCH.md
  - src/templates/DECISION-LOG.md
  - src/templates/SESSION.md
  - src/templates/INVARIANTS.md
  - src/templates/ACCEPTANCE-CRITERIA.md
  - src/templates/PERMISSIONS-MATRIX.md
  - src/templates/EDGE-CASES.md
  - src/templates/STATE-MACHINES.md
  - src/templates/REGRESSIONS.md
  - src/templates/steering.md
  - src/schemas/project-state.schema.json
  - src/schemas/preset-registry.schema.json
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - TMPL-06
  - TMPL-07
  - TMPL-08
  - TMPL-09
  - TMPL-10
  - TMPL-11
  - TMPL-12
  - TMPL-13

must_haves:
  truths:
    - "All 12 Markdown templates exist in src/templates/ with YAML frontmatter and required sections"
    - "steering.md is under 376 words (500-token proxy)"
    - "project-state.schema.json validates correct state objects and rejects invalid phase/policy_mode values"
    - "preset-registry.schema.json validates registry structure and allows additional preset fields"
    - "tests/templates.test.ts and tests/schemas.test.ts pass GREEN after this plan"
    - "steering.md and SESSION.md preserve route-summary and usage-intelligence hooks without expanding token cost"
  artifacts:
    - path: "src/templates/PRD.md"
      provides: "PRD template with 7 required sections"
      contains: "problem"
    - path: "src/templates/COMPILED-SPEC.md"
      provides: "COMPILED-SPEC template with 7 frontmatter fields"
      contains: "spec_version"
    - path: "src/templates/steering.md"
      provides: "Project constitution template under 500 tokens"
      contains: "intent"
    - path: "src/schemas/project-state.schema.json"
      provides: "JSON Schema draft-07 for project-state.json"
      exports: ["$schema", "required", "properties"]
    - path: "src/schemas/preset-registry.schema.json"
      provides: "JSON Schema draft-07 for preset registry"
      exports: ["$schema", "required", "properties"]
  key_links:
    - from: "src/templates/"
      to: "tests/templates.test.ts"
      via: "fs.readFileSync checks in test suite"
      pattern: "TEMPLATES_DIR"
    - from: "src/schemas/project-state.schema.json"
      to: "tests/schemas.test.ts"
      via: "Ajv compile and validate calls"
      pattern: "project-state.schema.json"
---

<objective>
Create all 12 Markdown governance artifact templates and 2 JSON schemas so that tests/templates.test.ts and tests/schemas.test.ts turn GREEN.

Purpose: The templates are the core deliverable of Phase 1 — every downstream agent and workflow consumes them. They must be machine-precision (D-01/D-02): YAML frontmatter with every required field, terse structured directives, no prose explanations, optimized for token efficiency.

The template set also needs to carry the automatic-effort-router posture: steering, session, and validation artifacts should expose route summaries and usage hooks so later commands can stay delta-based and cheap by default.

Output: 12 template files in src/templates/, 2 JSON schema files in src/schemas/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Required fields from tests — executor must satisfy these exactly. -->
<!-- Source: tests/templates.test.ts and tests/schemas.test.ts (written in plan 01-01) -->

tests/templates.test.ts checks these for each template:
- File exists at src/templates/{name}
- Content starts with `---\n` (YAML frontmatter opening)
- Closing `\n---` exists

PRD.md must contain all of: problem, actors, desired_outcomes, non_goals, constraints, success_criteria, open_questions
COMPILED-SPEC.md frontmatter must contain: spec_version, project, phase, requirements, protected, last_updated, source_refs
TEST-ARCH.md must contain: requirement_to_test_layer, rationale, fixture_needs, mock_policy, ci_tier
DECISION-LOG.md must contain: decision_id, date, author, spec_ref, change_type, rationale, impact, status
steering.md frontmatter must contain: version, project, phase, policy_mode
steering.md must contain sections: intent, non_negotiables, agent_rules, scope_boundaries, change_control
steering.md word count must be < 376 words
INVARIANTS.md must contain: invariant_id, scope, failure_severity
ACCEPTANCE-CRITERIA.md must contain: req_id
EDGE-CASES.md must contain: failure_mode, expected_behavior, severity, spec_ref
REGRESSIONS.md must contain: bug_summary, root_cause, test_added, spec_ref
STATE-MACHINES.md must contain: entity, transitions, invalid_transitions

tests/schemas.test.ts checks:
- project-state.schema.json: required includes [phase, spec_hash, active_slice, last_session, policy_mode]
- project-state.schema.json: phase enum = [intake, interrogation, spec-compilation, test-architecture, baseline-protection, implementation, adversarial-review, regression-capture, handoff]
- project-state.schema.json: policy_mode enum = [strict, standard, lightweight]
- project-state.schema.json: additionalProperties NOT false (must allow extra fields)
- preset-registry.schema.json: required includes [version, presets]
</interfaces>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Vector | Mitigation | Severity |
|--------|--------|------------|----------|
| Template injection via placeholder strings | Agent substitutes `{{PLACEHOLDER}}` values with malicious content in future runs | Placeholders use double-brace syntax — this is intentional. Templates are consumed by agents under controlled conditions. No eval or code execution from template content. LOW risk. | LOW |
| Schema drift breaking Phase 2 validation | Strict `additionalProperties: false` in Phase 1 schema blocks Phase 2 field additions | Schema uses `required` to enforce minimum fields and `additionalProperties: true` (default) to allow extension. Explicitly tested. | MEDIUM — blocked by test |
| Token budget violation in steering.md | Template grows over 500 tokens, increasing agent context cost | Test asserts word count < 376. Each directive is one line per D-05. | MEDIUM — blocked by test |

No code execution in this plan. Templates and schemas are static files.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write all 12 Markdown governance artifact templates</name>
  <files>
    src/templates/PRD.md,
    src/templates/COMPILED-SPEC.md,
    src/templates/TEST-ARCH.md,
    src/templates/DECISION-LOG.md,
    src/templates/SESSION.md,
    src/templates/INVARIANTS.md,
    src/templates/ACCEPTANCE-CRITERIA.md,
    src/templates/PERMISSIONS-MATRIX.md,
    src/templates/EDGE-CASES.md,
    src/templates/STATE-MACHINES.md,
    src/templates/REGRESSIONS.md,
    src/templates/steering.md
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/tests/templates.test.ts (MANDATORY — read the exact assertion strings before writing any template)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-05 constraints)
    - /Users/jakyeamos/projects/Terrace/.planning/REQUIREMENTS.md (lines 151 to 179 for exact TMPL field specifications)
  </read_first>
  <action>
Create `src/templates/` directory. Write all 12 templates with machine-precision format (D-01): YAML frontmatter for metadata, terse single-line structured directives, no prose explanations. Optimize for token efficiency. Every field defined, every section present, agent hints as metadata comments.

Design rule: each directive entry is one line. No paragraph prose inside templates. Use `# hint: description` for agent-readable annotations.

--- src/templates/PRD.md ---
```markdown
---
version: "1.0"
artifact: PRD
spec_ref: "{{SPEC-XX}}"
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
status: draft
---

## problem
# One-line problem statement. What is broken or missing?

## actors
# - actor_id: description (role, system, or external party)

## desired_outcomes
# - outcome_id: measurable result when problem is solved

## non_goals
# - explicitly out of scope (prevents scope creep)

## constraints
# - constraint_id: hard limit on solution space (time, budget, tech, regulatory)

## success_criteria
# - criterion_id: observable, verifiable condition
# spec_ref: SPEC-XX

## open_questions
# - question_id: unresolved decision needed before spec can close
# owner: {{OWNER}}
# due: {{ISO_DATE}}
```

--- src/templates/COMPILED-SPEC.md ---
```markdown
---
spec_version: "1.0"
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
requirements:
  - "{{REQ-ID}}"
protected: false
last_updated: "{{ISO_DATE}}"
source_refs:
  - "docs/prd/PRD.md"
---

## spec_summary
# One-line description of what this spec governs.

## behavioral_truths
# - SPEC-XX: observable behavior that must always hold

## invariants
# - INV-XX: system-level truth; scope: {{SCOPE}}; severity: {{blocking|warning}}

## permissions
# - actor: {{ACTOR}}; can: [read, write, delete]; cannot: [action]

## edge_cases
# - EDGE-XX: failure_mode; expected_behavior; severity; spec_ref: SPEC-XX

## state_machines
# entity: {{ENTITY}}
# states: [state_a, state_b]
# transitions: state_a -> state_b on {{EVENT}}
# invalid_transitions: [state_b -> state_a]

## open_questions
# - question_id: unresolved; owner: {{OWNER}}; due: {{ISO_DATE}}
```

--- src/templates/TEST-ARCH.md ---
```markdown
---
version: "1.0"
artifact: TEST-ARCH
spec_ref: "{{SPEC-XX}}"
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## requirement_to_test_layer
# req_id | test_layer | file | status
# {{REQ-ID}} | unit | tests/{{module}}.test.ts | pending

## rationale
# - test_layer: reason this layer was chosen for req_id

## fixture_needs
# - fixture_id: description; shared: true|false; file: tests/fixtures/{{name}}

## mock_policy
# - what_to_mock: external services, DB calls, filesystem in unit layer
# - what_not_to_mock: business logic, pure functions, transition validators

## ci_tier
# - tier_1: unit — runs on every commit; target < 30s
# - tier_2: integration — runs on PR; target < 120s
# - tier_3: e2e — runs on merge to main; target < 300s
```

--- src/templates/DECISION-LOG.md ---
```markdown
---
version: "1.0"
artifact: DECISION-LOG
project: "{{PROJECT_NAME}}"
last_updated: "{{ISO_DATE}}"
---

## entries

### {{decision_id}}
decision_id: "{{D-XX}}"
date: "{{ISO_DATE}}"
author: "{{AUTHOR}}"
spec_ref: "{{SPEC-XX}}"
change_type: "{{add|modify|remove|defer}}"
rationale: "{{one-line rationale}}"
impact: "{{files or behaviors affected}}"
status: "{{active|superseded|deferred}}"
```

--- src/templates/SESSION.md ---
```markdown
---
version: "1.0"
artifact: SESSION
project: "{{PROJECT_NAME}}"
session_id: "{{ISO_DATETIME}}"
phase: "{{CURRENT_PHASE}}"
spec_hash: "{{HASH_OR_NULL}}"
policy_mode: "{{strict|standard|lightweight}}"
---

## session_start
# phase: {{CURRENT_PHASE}}
# spec_hash: {{HASH}}
# active_slice: {{SLICE_OR_NULL}}
# previous_session: {{ISO_DATETIME_OR_NULL}}

## files_changed
# - path: {{file}}; change_type: {{add|modify|delete}}; spec_ref: {{SPEC-XX}}

## decisions_made
# - decision_id: {{D-XX}}; spec_ref: {{SPEC-XX}}; rationale: {{one-line}}

## risks_identified
# - risk_id: {{R-XX}}; description: {{one-line}}; severity: {{blocking|warning}}

## protected_artifact_changes
# - file: {{path}}; decision_ref: {{D-XX}}; spec_ref: {{SPEC-XX}}

## next_slice
# {{description of what to do next session}}
```

--- src/templates/INVARIANTS.md ---
```markdown
---
version: "1.0"
artifact: INVARIANTS
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## invariants

### {{invariant_id}}
invariant_id: "{{INV-XX}}"
scope: "{{module, subsystem, or global}}"
failure_severity: "{{blocking|warning}}"
description: "{{one-line truth that must always hold}}"
spec_ref: "{{SPEC-XX}}"
test_anchor: "{{test file path or MISSING}}"
```

--- src/templates/ACCEPTANCE-CRITERIA.md ---
```markdown
---
version: "1.0"
artifact: ACCEPTANCE-CRITERIA
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## criteria

### {{req_id}}
req_id: "{{REQ-ID}}"
spec_ref: "{{SPEC-XX}}"
criterion: "{{observable, verifiable condition — no subjective language}}"
verification_method: "{{automated_test|manual_check|cli_output}}"
status: "{{pending|passing|failing}}"
```

--- src/templates/PERMISSIONS-MATRIX.md ---
```markdown
---
version: "1.0"
artifact: PERMISSIONS-MATRIX
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## matrix

# actor | resource | read | write | delete | conditions
# {{ACTOR}} | {{RESOURCE}} | yes|no | yes|no | yes|no | {{condition or none}}

## entries

### {{actor_id}}
actor: "{{actor_id}}"
resource: "{{resource}}"
read: "{{yes|no}}"
write: "{{yes|no}}"
delete: "{{yes|no}}"
conditions: "{{condition or none}}"
spec_ref: "{{SPEC-XX}}"
```

--- src/templates/EDGE-CASES.md ---
```markdown
---
version: "1.0"
artifact: EDGE-CASES
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## cases

### {{case_id}}
case_id: "{{EDGE-XX}}"
failure_mode: "{{what goes wrong}}"
expected_behavior: "{{what the system must do instead}}"
severity: "{{blocking|warning|informational}}"
spec_ref: "{{SPEC-XX}}"
test_ref: "{{test file::test name or MISSING}}"
```

--- src/templates/STATE-MACHINES.md ---
```markdown
---
version: "1.0"
artifact: STATE-MACHINES
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
last_updated: "{{ISO_DATE}}"
---

## machines

### {{entity}}
entity: "{{entity_name}}"
initial_state: "{{state}}"
states:
  - "{{state_a}}"
  - "{{state_b}}"

transitions:
  - from: "{{state_a}}"
    to: "{{state_b}}"
    on: "{{event}}"
    guard: "{{condition or none}}"
    spec_ref: "{{SPEC-XX}}"

invalid_transitions:
  - from: "{{state_b}}"
    to: "{{state_a}}"
    reason: "{{why this is illegal}}"
```

--- src/templates/REGRESSIONS.md ---
```markdown
---
version: "1.0"
artifact: REGRESSIONS
project: "{{PROJECT_NAME}}"
last_updated: "{{ISO_DATE}}"
---

## regressions

### {{regression_id}}
regression_id: "{{REG-XX}}"
bug_summary: "{{one-line description of what broke}}"
root_cause: "{{why it broke}}"
test_added: "{{test file::test name}}"
spec_ref: "{{SPEC-XX}}"
date_found: "{{ISO_DATE}}"
date_resolved: "{{ISO_DATE_OR_NULL}}"
```

--- src/templates/steering.md ---
CRITICAL: This file must stay under 376 words (500-token proxy per D-05). Count words after writing. Each directive is ONE LINE — no paragraph prose.

```markdown
---
version: "1.0"
project: "{{PROJECT_NAME}}"
phase: "{{CURRENT_PHASE}}"
policy_mode: standard
---

## intent
# {{One-line project goal}}.

## non_negotiables
# - inv_01: {{invariant description}}
# - inv_02: {{invariant description}}

## agent_rules
# - rule_01: load steering.md before any workflow step
# - rule_02: never modify protected files without decision_id and spec_ref
# - rule_03: all state writes go through terrace-tools.cjs structured write path
# - rule_04: surface conflicts explicitly — no silent overwrites

## scope_boundaries
# in_scope: [{{item}}, {{item}}]
# out_of_scope: [{{item}}, {{item}}]

## change_control
# protected_files: []
# decision_log_required: true
# spec_ref_required: true
# policy_mode_changes: require_explicit_flag
```

After writing, count words in steering.md. If word count >= 376, trim comments until under the limit.
  </action>
  <verify>
    <automated>npx vitest run tests/templates.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/templates/` shows 12 files: PRD.md, COMPILED-SPEC.md, TEST-ARCH.md, DECISION-LOG.md, SESSION.md, INVARIANTS.md, ACCEPTANCE-CRITERIA.md, PERMISSIONS-MATRIX.md, EDGE-CASES.md, STATE-MACHINES.md, REGRESSIONS.md, steering.md
    - `head -1 src/templates/PRD.md` outputs `---` (YAML frontmatter opening)
    - `grep "desired_outcomes" src/templates/PRD.md` returns a match
    - `grep "spec_version" src/templates/COMPILED-SPEC.md` returns a match
    - `grep "intent" src/templates/steering.md` returns a match
    - `grep "non_negotiables" src/templates/steering.md` returns a match
    - `wc -w src/templates/steering.md` outputs a number less than 376
    - `npx vitest run tests/templates.test.ts` exits 0 (GREEN)
  </acceptance_criteria>
  <done>All 12 template files exist; tests/templates.test.ts passes GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write JSON schemas for project-state.json and preset-registry.json</name>
  <files>
    src/schemas/project-state.schema.json,
    src/schemas/preset-registry.schema.json
  </files>
  <read_first>
    - /Users/jakyeamos/projects/Terrace/tests/schemas.test.ts (MANDATORY — read exact assertions before writing schema)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/01-foundation/01-CONTEXT.md (D-11 and D-13 schema specifications)
    - /Users/jakyeamos/projects/Terrace/.planning/phases/01-foundation/01-RESEARCH.md (JSON Schema draft-07 example and Pitfall 4 on additionalProperties)
  </read_first>
  <action>
Create `src/schemas/` directory. Write exactly two JSON schema files.

--- src/schemas/project-state.schema.json ---
Schema must enforce:
- `$schema`: "http://json-schema.org/draft-07/schema#"
- `required`: ["phase", "spec_hash", "active_slice", "last_session", "policy_mode"]
- `phase` enum: exactly these 9 values: "intake", "interrogation", "spec-compilation", "test-architecture", "baseline-protection", "implementation", "adversarial-review", "regression-capture", "handoff"
- `policy_mode` enum: exactly these 3 values: "strict", "standard", "lightweight"
- `spec_hash`: type ["string", "null"]
- `active_slice`: type ["string", "null"]
- `last_session`: type ["string", "null"] with format "date-time"
- `additionalProperties`: true (NOT false — Pitfall 4: must allow Phase 2+ field additions)

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TerraceProjectState",
  "description": "Machine-readable lifecycle state for a Terrace-governed workstream",
  "type": "object",
  "required": ["phase", "spec_hash", "active_slice", "last_session", "policy_mode"],
  "additionalProperties": true,
  "properties": {
    "phase": {
      "type": "string",
      "enum": [
        "intake",
        "interrogation",
        "spec-compilation",
        "test-architecture",
        "baseline-protection",
        "implementation",
        "adversarial-review",
        "regression-capture",
        "handoff"
      ]
    },
    "spec_hash": {
      "type": ["string", "null"],
      "description": "SHA-256 of COMPILED-SPEC.md at last session, or null if no spec compiled yet"
    },
    "active_slice": {
      "type": ["string", "null"],
      "description": "Current work slice identifier, or null if no active slice"
    },
    "last_session": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "ISO-8601 timestamp of last session end, or null if no sessions recorded"
    },
    "policy_mode": {
      "type": "string",
      "enum": ["strict", "standard", "lightweight"],
      "description": "Enforcement strictness level for governance gates"
    }
  }
}
```

--- src/schemas/preset-registry.schema.json ---
Schema must enforce:
- `$schema`: "http://json-schema.org/draft-07/schema#"
- `required`: ["version", "presets"]
- `presets`: array of preset entries
- Each preset entry `required`: ["id", "name", "version", "category", "effect", "installed_at", "conflicts"]
- `category` enum: ["governance", "testing", "frontend", "security", "integration"]
- `effect` enum: ["Read-only", "Read+Write"]
- `additionalProperties`: true on preset entries (to allow agents, workflows, fragments, flags in Phase 2+)

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TerracePresetRegistry",
  "description": "Machine-readable catalog of installed Terrace presets",
  "type": "object",
  "required": ["version", "presets"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "description": "Registry schema version"
    },
    "presets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "version", "category", "effect", "installed_at", "conflicts"],
        "additionalProperties": true,
        "properties": {
          "id": { "type": "string", "description": "Unique preset identifier (e.g. terrace-tea)" },
          "name": { "type": "string" },
          "version": { "type": "string" },
          "category": {
            "type": "string",
            "enum": ["governance", "testing", "frontend", "security", "integration"]
          },
          "effect": {
            "type": "string",
            "enum": ["Read-only", "Read+Write"]
          },
          "installed_at": { "type": "string", "format": "date-time" },
          "conflicts": {
            "type": "array",
            "items": { "type": "string" },
            "description": "IDs of presets that conflict with this one"
          }
        }
      }
    }
  }
}
```
  </action>
  <verify>
    <automated>npx vitest run tests/schemas.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/schemas/` shows exactly 2 files: project-state.schema.json, preset-registry.schema.json
    - `grep "\"required\"" src/schemas/project-state.schema.json` returns a match
    - `grep "additionalProperties" src/schemas/project-state.schema.json` shows `true` (not false)
    - `grep "intake" src/schemas/project-state.schema.json` returns a match (phase enum present)
    - `grep "strict" src/schemas/project-state.schema.json` returns a match (policy_mode enum present)
    - `npx vitest run tests/schemas.test.ts` exits 0 (GREEN)
  </acceptance_criteria>
  <done>Both schema files exist; tests/schemas.test.ts passes GREEN; project-state schema allows additional properties</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/templates.test.ts tests/schemas.test.ts --reporter=verbose` exits 0
2. `ls src/templates/ | wc -l` outputs 12
3. `ls src/schemas/ | wc -l` outputs 2
4. `wc -w src/templates/steering.md` outputs a number less than 376
5. `grep -c "it(" tests/templates.test.ts` confirms template assertions are untouched (RED tests from 01-01 now GREEN)
</verification>

<success_criteria>
- All 12 template files exist in src/templates/ with YAML frontmatter and required fields
- Both JSON schema files exist in src/schemas/ with correct structure
- tests/templates.test.ts passes GREEN
- tests/schemas.test.ts passes GREEN
- steering.md word count is under 376
- Remaining 6 test files (init, validate, lifecycle, preset, doctor, json-mode) remain RED — no implementation yet
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Template files created (list with word counts where relevant)
- steering.md word count (must confirm under 376)
- Schema files created
- Test files now GREEN vs still RED
- Any deviations from this plan
</output>
