# Contributing

Use a feature branch for non-trivial work. Keep commits scoped to one concern and update the project truth file after each implementation commit.

Before opening a PR, run:

```sh
npm run ci
```

Do not add generated local state, local agent settings, or planning archives to the npm package allowlist.
