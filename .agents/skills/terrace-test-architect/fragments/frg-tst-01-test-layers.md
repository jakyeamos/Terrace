# Test Layer Selection

Requirements map to one of four test layers: unit (pure logic), integration (module boundaries), contract (API surface), or e2e (user-visible behavior). Layer selection drives CI tier assignment.
