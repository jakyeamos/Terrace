# Spec Drift Detection Patterns

> Phase 6 stub — full implementation in Phase 6 adversarial review plans.

Compare implemented behavior against `docs/spec/COMPILED-SPEC.md`, not against summary claims. Drift includes missing requirement coverage, changed behavior without a spec update, stale derived artifacts, and formatting-only changes incorrectly treated as semantic changes. Phase 6 will expand this into executable review steps.
