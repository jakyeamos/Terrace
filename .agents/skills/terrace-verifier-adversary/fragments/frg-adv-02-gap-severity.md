# Gap Severity and Regression Requirements

> Phase 6 stub — full implementation in Phase 6 adversarial review plans.

Classify gaps as blocking when they violate protected requirements, auth, permissions, money flow, data loss, schema integrity, or phase success criteria. Non-blocking gaps require a documented follow-up target. Every confirmed gap should name the regression test needed to prevent recurrence.
