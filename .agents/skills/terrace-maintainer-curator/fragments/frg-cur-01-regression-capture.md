# Regression Capture Handoff

Every captured regression needs a `spec_ref`, observed failure, root cause, test added, and baseline registration status. If a regression cannot be protected immediately, record a decision-log entry with a future phase target before closing the session.
