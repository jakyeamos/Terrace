# Invariant Patterns

Invariants are system-wide constraints that must hold across all states. Each invariant entry in INVARIANTS.md names the constraint, its scope, and references the requirement that mandates it.
